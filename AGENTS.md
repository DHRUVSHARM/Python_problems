# Agent Instructions

- Do not run, execute, compile, syntax-check, or otherwise attempt to run any
  problem solution file in this repository unless the user explicitly asks for
  that action.
- For requests that only ask for summaries, dry runs, comments, explanations,
  or documentation, read and edit the relevant files only. Do not verify by
  running Python files, `py_compile`, tests, or similar commands unless the user
  specifically prompts for execution.
