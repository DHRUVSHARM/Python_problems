general notes after revision 

topological sort has 2 ways, 

1) dfs postorder, keep visited with true false toggle to check in path for cycle detection  , ans is reverse of output 
2) kahns? remove one node and incoming edges, should be able to remove one each iteration if not then cycle , ans is in order
if any node given then islands concept to iterate across nodes and make it work 
there is a reverse edges way not req


notes after revision: for problems 

course schedule - 1
already crs -> prereq is given as reverse graph, simple topo with cycle detect
course schedule - 2
same thing except we return the ordering 

REVIEW : 

sample dry run 

Input: numCourses = 3, prerequisites = [[0,1],[1,2],[2,0]]

Output: []

0 : 1
1 : 2
2 : 0

indegree , none 
0 : 1
1 : 1
2 : 1

we can use queue and then once the queue is empty then that means we can check 
for disconnected components, it is important that we iterate over all the componenets

Kahn's Algorithm works by:

Always taking courses with no remaining prerequisites (indegree = 0)
Removing them from the graph
Gradually unlocking other courses
If at the end some courses are still locked, it means a cycle exists, so no valid order is possible.

Algorithm
Build a graph and compute indegree for each course
(indegree = number of prerequisites).
Add all courses with indegree = 0 to a queue.
While the queue is not empty:
Remove a course and add it to the result.
Reduce the indegree of its dependent courses.
If any dependent course reaches indegree = 0, add it to the queue.
If all courses are processed, return the result (reverse if edges were stored as course - prerequisite).
Otherwise, return an empty list (cycle detected).

course schedule 4

answering queries with similar concept
Input: numCourses = 4, prerequisites = [[1,0],[2,1],[3,2]], queries = [[0,1],[3,1]]

3-> 2 -> 1 -> 0

Output: [false,true]
we can do floyd warshall but will be cubic in vertices

do 
alien dictionary 
sort respecting dependencies